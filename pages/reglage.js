import React from 'react';
import Layout from '../components/Layout';
import styles from '@/styles/dashboard.module.css'

function Reglages() {
  const pageTitle = 'Reglages';

  return (
    <Layout pageTitle={pageTitle}>
    </Layout>
  );
}

export default Reglages;